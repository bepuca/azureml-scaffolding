[tool.black]
line-length = 100
include = '\.pyi?$'
exclude = '''
    /(
        \.git
      | _build
      | build
      | dist
    ) '''

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
# None for the moment